function formatString(str) {
    return str.trim().toLowerCase();
}

function addNumbers(a, b) {
    return a + b;
}

function multiplyNumbers(a, b) {
    return a * b;
}

export { formatString, addNumbers, multiplyNumbers };